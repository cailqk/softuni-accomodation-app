{{#with place}}

<h1>Details for: {{name}}</h1>
<main class="main_details">
    <div>
    <img class="card-img-top" style="width: 25rem" src="{{imgUrl}}">
    </div>
    <p>{{description}}</p>
    <p>Pricing: ${{price}}</p>
    <div>
        <a class="btn btn-primary" href="/extras/{{_id}}/addToRoom">Edit Extras</a>
    </div>
</main>
    <footer>Listed by: ADD HOST</footer>
{{/with}}